#include <iostream>
#include <cmath>

using namespace std;

int main(){
    int a, b;
    cin >> a >> b;
    unordered_map<int, int> map;
    for (int i = 0; i < a; i++) {
        int ci, ti;
        cin >> ci >> ti;
        if (ti <= b) {
            map[ti] <
        }
    }
}